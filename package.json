{
  "name": "chessking",
  "version": "1.0.0",
  "description": "ChessKing Android App",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p dist && cp *.html *.css *.js dist/"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  }
}

{
  "appId": "com.yourcompany.yourapp",
  "appName": "actor",
  "webDir": "dist",
  "bundledWebRuntime": false,
  "server": {
    "androidScheme": "https"
  },
  "plugins": {
    "Filesystem": {
      // optional: set default directory if needed
    },
    "Share": {
      // optional: share plugin config
    }
  }
}
